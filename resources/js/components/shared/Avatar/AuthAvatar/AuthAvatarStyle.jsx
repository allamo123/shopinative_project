import { makeStyles } from "@material-ui/core";

export const AuthAvatarStyle = makeStyles((theme) => ({
    small: {
        width: 40,
        height: 40,
        border: '1px solid #ddd'
    }
}));
